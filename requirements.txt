# Core libraries
pandas>=2.0.0
numpy>=1.25.0

# HTTP and retry
requests>=2.31.0
urllib3<2.0

# Cache (optional but recommended for API retries)
requests-cache>=0.9.8

# Open-Meteo API client
openmeteo-requests==1.7.4

# Retry logic for network calls
retry-requests>=0.3.2
